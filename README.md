Publish hacked-up versions of some npm modules, made to be smaller, sometimes
at the cost of some functionality. Typically these are published to
`@smaller/$origPackageName`, e.g. `@smaller/tap`.


## notes

- "@smaller" npm org